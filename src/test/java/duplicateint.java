import java.util.*;
import java.util.stream.Collectors;

import static java.util.Arrays.sort;

public class duplicateint {
    public static void main(String[] args) {
       /* ArrayList<Integer> list = new ArrayList<>(Arrays.asList(1, 5, 2, 1, 5, 6, 8, 6, 8, 9, 785, 45, 7));
         Arrays.sort(new ArrayList[]{list});

        Collections.sort(list)  ;

         System.out.println  ( "" +list) ;
        List<Integer> du = list.stream().distinct().collect(Collectors.toList());
        System.out.println(du);}}*/
        //ArrayList<String> list = new ArrayList<String>();

        // Form a list of numbers from 0-9.


        // Insert a new set of numbers from 0-5.



        /*HashSet<String> hset =
                new HashSet<String>(Arrays.asList(new String[]{"Volvo", "BMW", "Ford", "Mazda", "BMW", "Ford"}));
        System.out.println( hset );*/


        /* ArrayList<String> list = new ArrayList<String>();

         Form a list of numbers from 0-9.
        for (int i = 0; i < 10; i++) {
            list.add(String.valueOf(i));
        }/*

        // Insert a new set of numbers from 0-5.
        for (int i = 0; i < 5; i++) {
            list.add(String.valueOf(i));
        }

        System.out.println("Input list : " + list);
        System.out.println("\nFiltered duplicates : " + processList(list));
    }

    public static Set<String> processList(List<String> listContainingDuplicates) {

        final Set<String> resultSet = new HashSet<String>();
        final Set<String> tempSet = new HashSet<String>();

        for (String yourInt : listContainingDuplicates) {
            if (!tempSet.add(yourInt)) {
                resultSet.add(yourInt);
            }
        }
        return resultSet;
    }*/
    }}








      /* String[] cars = {"Volvo", "BMW", "Ford", "Mazda", "BMW", "Ford"};
        HashSet set = new HashSet<>(cars);
        cars.clear();
        cars.addAll(set);
    //}
      //  String inp = "HI";*/



  /* public static boolean ShowList(ArrayList<Integer> list) {


       for (Integer str : list) {
           System.out.println(str);

       }
       //System.out.println();
   }
*?/*/


